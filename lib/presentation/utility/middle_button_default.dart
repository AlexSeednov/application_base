/// Non-web branch of the autoscroll's hold on the middle button: outside a
/// browser nothing acts on that button by default, so there is nothing to
/// hold off.
void holdMiddleButtonDefault({required bool isHeld}) {}
