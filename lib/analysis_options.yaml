# The rule set applications on this stack include with
#   include: package:application_base/analysis_options.yaml
# A rule's documentation: https://dart.dev/tools/linter-rules/<rule>.
# `Experimental.` marks a rule the SDK may still change or drop — the first
# suspect when analysis breaks after an SDK upgrade.
analyzer:
  # Generated code is not ours to fix. Excluding it also silences
  # invalid_use_of_visible_for_testing_member in the injectable modules
  # (*.module.dart / *.config.dart): they call the @visibleForTesting service
  # constructors.
  exclude:
    - '**/*.g.dart'
    - '**/*.gr.dart'
    - '**/*.gen.dart'
    - '**/*.config.dart'
    - '**/*.module.dart'

  # https://dart.dev/tools/analysis#enabling-additional-type-checks
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

linter:
  rules:

    # Error rules: https://dart.dev/tools/linter-rules#error-rules

    always_use_package_imports: true
    avoid_dynamic_calls: true
    avoid_empty_else: true

    # Experimental.
    avoid_futureor_void: true

    avoid_print: true

    # Narrower than always_use_package_imports, enabled above.
    avoid_relative_lib_imports: false

    avoid_slow_async_io: true
    avoid_type_to_string: true
    avoid_types_as_parameter_names: true
    avoid_web_libraries_in_flutter: true
    cancel_subscriptions: true
    close_sinks: true
    collection_methods_unrelated_type: true
    comment_references: true
    control_flow_in_finally: true
    deprecated_member_use_from_same_package: true

    # Pays off only where the UI is debugged through the Flutter Inspector
    # property view. Everywhere else it is debugFillProperties boilerplate on
    # every Diagnosticable, paid whether anyone reads it or not.
    diagnostic_describe_all_properties: false

    # Does not complement unawaited_futures the way it seems to. A missing await
    # in an async body is already caught there, and an async callback handed to
    # a synchronous higher-order function — where the future really is lost —
    # goes unreported here. What it reports is the deliberate kind: cancelling a
    # subscription or closing a sink in dispose, a storage write left to flush
    # on its own, a navigation call. Those sit in synchronous functions, so the
    # only fix is to spell out unawaited(): ceremony, not risk.
    discarded_futures: false

    empty_statements: true
    hash_and_equals: true

    # Experimental.
    implicit_reopen: true

    # Experimental.
    invalid_case_patterns: true

    invalid_runtime_check_with_js_interop_types: true
    literal_only_boolean_expressions: true
    missing_code_block_language_in_doc_comment: true
    no_adjacent_strings_in_list: true
    no_duplicate_case_values: true
    no_logic_in_create_state: true
    no_self_assignments: true
    no_wildcard_variable_uses: true

    # Incompatible with always_use_package_imports, enabled above: code is
    # always imported through `package:` URIs.
    prefer_relative_imports: false

    prefer_void_to_null: true
    switch_on_type: true
    test_types_in_equals: true
    throw_in_finally: true
    unintended_html_in_doc_comment: true
    unnecessary_statements: true
    unrelated_type_equality_checks: true

    # Experimental.
    # Fires on the ordinary shape of a generic widget or view model that takes a
    # callback: a field typed `Widget Function(BuildContext, T, int)` puts T in
    # a parameter position — unsound in theory, the whole Flutter builder idiom
    # in practice. No rewrite keeps the API and satisfies the rule, so it would
    # only ever be silenced.
    unsafe_variance: false

    use_build_context_synchronously: true
    use_key_in_widget_constructors: true
    valid_regexps: true

    # Style rules: https://dart.dev/tools/linter-rules#style-rules

    always_declare_return_types: true

    # Conflicts with the guard-clause style (`if (isOffline) return;`). The real
    # hazard — a body that looks attached but is not — is covered by
    # curly_braces_in_flow_control_structures.
    always_put_control_body_on_new_line: false

    always_put_required_named_parameters_first: true

    # Incompatible with avoid_types_on_closure_parameters (enabled) and
    # omit_local_variable_types. It would also demand a type where the
    # initializer already names it: `final value = SomeType();`.
    always_specify_types: false

    annotate_overrides: true

    # Experimental.
    annotate_redeclares: true

    avoid_annotating_with_dynamic: true
    avoid_bool_literals_in_conditional_expressions: true

    # The data layer ends every request in a catch-all on purpose, so that no
    # exception escapes to the layers above (the "null on error" contract).
    avoid_catches_without_on_clauses: false

    avoid_catching_errors: true

    # Would flag the `abstract final class` namespaces used on purpose
    # (SafeService, SecureStorageUtility, ApplicationBase): the rule predates
    # `abstract final class`.
    avoid_classes_with_only_static_members: false

    avoid_double_and_int_checks: true
    avoid_equals_and_hash_code_on_mutable_classes: true

    # Quoting is left to prefer_single_quotes: an escaped inner `'` is fine.
    avoid_escaping_inner_quotes: false

    avoid_field_initializers_in_const_classes: true
    avoid_final_parameters: true
    avoid_function_literals_in_foreach_calls: true
    avoid_implementing_value_types: true
    avoid_init_to_null: true
    avoid_js_rounded_ints: true
    avoid_multiple_declarations_per_line: true
    avoid_positional_boolean_parameters: true
    avoid_private_typedef_functions: true
    avoid_redundant_argument_values: true
    avoid_renaming_method_parameters: true
    avoid_return_types_on_setters: true
    avoid_returning_null_for_void: true
    avoid_returning_this: true

    # A write-only setter over private state is a legitimate API: set from
    # outside, read only inside.
    avoid_setters_without_getters: false

    avoid_shadowing_type_parameters: true
    avoid_single_cascade_in_expression_statements: true
    avoid_types_on_closure_parameters: true
    avoid_unnecessary_containers: true
    avoid_unused_constructor_parameters: true
    avoid_void_async: true
    await_only_futures: true
    camel_case_extensions: true
    camel_case_types: true
    cascade_invocations: true
    cast_nullable_to_non_nullable: true
    combinators_ordering: true
    conditional_uri_does_not_exist: true
    constant_identifier_names: true
    curly_braces_in_flow_control_structures: true
    dangling_library_doc_comments: true
    deprecated_consistency: true
    directives_ordering: true
    do_not_use_environment: true
    document_ignores: true
    empty_catches: true
    empty_constructor_bodies: true
    eol_at_end_of_file: true
    exhaustive_cases: true
    file_names: true
    flutter_style_todos: true
    implementation_imports: true
    implicit_call_tearoffs: true
    join_return_with_assignment: true
    leading_newlines_in_multiline_strings: true
    library_annotations: true
    library_names: true
    library_prefixes: true
    library_private_types_in_public_api: true
    lines_longer_than_80_chars: true
    matching_super_parameters: true
    missing_whitespace_between_adjacent_strings: true

    # Experimental.
    no_default_cases: true

    no_leading_underscores_for_library_prefixes: true
    no_leading_underscores_for_local_identifiers: true
    no_literal_bool_comparisons: true
    no_runtimetype_tostring: true
    non_constant_identifier_names: true
    noop_primitive_operations: true
    null_check_on_nullable_type_parameter: true
    null_closures: true

    # The codebase annotates local variables explicitly.
    omit_local_variable_types: false

    # Same decision as omit_local_variable_types above.
    omit_obvious_local_variable_types: false

    # Incompatible with type_annotate_public_apis, enabled below: public API
    # stays explicitly typed.
    omit_obvious_property_types: false

    one_member_abstracts: true
    only_throw_errors: true
    overridden_fields: true
    package_prefixed_library_names: true
    parameter_assignments: true
    prefer_adjacent_string_concatenation: true
    prefer_asserts_in_initializer_lists: true
    prefer_asserts_with_message: true
    prefer_collection_literals: true
    prefer_conditional_assignment: true
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_constructors_over_static_methods: true
    prefer_contains: true

    # Incompatible with prefer_single_quotes, enabled below.
    prefer_double_quotes: false

    # Would force `=>` on every single-return body, `build` methods included,
    # where a block body often reads better.
    prefer_expression_function_bodies: false

    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true
    prefer_for_elements_to_map_fromiterable: true
    prefer_foreach: true
    prefer_function_declarations_over_variables: true
    prefer_generic_function_type_aliases: true
    prefer_if_elements_to_conditional_expressions: true
    prefer_if_null_operators: true
    prefer_initializing_formals: true
    prefer_inlined_adds: true
    prefer_int_literals: true
    prefer_interpolation_to_compose_strings: true
    prefer_is_empty: true
    prefer_is_not_empty: true
    prefer_is_not_operator: true
    prefer_iterable_wheretype: true
    prefer_mixin: true
    prefer_null_aware_method_calls: true
    prefer_null_aware_operators: true
    prefer_single_quotes: true
    prefer_spread_collections: true
    prefer_typing_uninitialized_variables: true
    provide_deprecation_message: true

    # Enforces the project rule that every declaration carries a `///` comment.
    # An empty `///` satisfies it: the point is that adding a public member is a
    # deliberate act, not that every line needs prose.
    public_member_api_docs: true

    recursive_getters: true
    remove_deprecations_in_breaking_versions: true
    require_trailing_commas: true
    simplify_variable_pattern: true
    sized_box_for_whitespace: true
    sized_box_shrink_expand: true
    slash_for_doc_comments: true
    sort_child_properties_last: true
    sort_constructors_first: true
    sort_unnamed_constructors_first: true

    # Local types are a habit here, not a requirement: this rule would report
    # style rather than risk, and at the volume it fires it buries the findings
    # that matter. Types that are API surface — fields and signatures — stay
    # enforced by specify_nonobvious_property_types and
    # type_annotate_public_apis.
    specify_nonobvious_local_variable_types: false

    specify_nonobvious_property_types: true
    strict_top_level_inference: true
    tighten_type_of_initializing_formals: true
    type_annotate_public_apis: true
    type_init_formals: true
    type_literal_in_constant_pattern: true
    unawaited_futures: true

    # Experimental.
    unnecessary_async: true

    unnecessary_await_in_return: true
    unnecessary_brace_in_string_interps: true
    unnecessary_breaks: true
    unnecessary_const: true
    unnecessary_constructor_name: true

    # Incompatible with prefer_final_locals, enabled above.
    unnecessary_final: false

    unnecessary_getters_setters: true
    unnecessary_ignore: true
    unnecessary_lambdas: true
    unnecessary_late: true
    unnecessary_library_directive: true
    unnecessary_library_name: true
    unnecessary_new: true
    unnecessary_null_aware_assignments: true
    unnecessary_null_aware_operator_on_extension_on_nullable: true

    # Experimental.
    unnecessary_null_checks: true

    unnecessary_null_in_if_null_operators: true
    unnecessary_nullable_for_final_variable_declarations: true
    unnecessary_overrides: true
    unnecessary_parenthesis: true
    unnecessary_raw_strings: true
    unnecessary_string_escapes: true
    unnecessary_string_interpolations: true
    unnecessary_this: true
    unnecessary_to_list_in_spreads: true
    unnecessary_unawaited: true
    unnecessary_underscores: true
    unreachable_from_main: true
    use_colored_box: true
    use_decorated_box: true
    use_enums: true
    use_full_hex_values_for_flutter_colors: true
    use_function_type_syntax_for_parameters: true
    use_is_even_rather_than_modulo: true

    # Experimental.
    use_late_for_private_fields_and_variables: true

    use_named_constants: true
    use_null_aware_elements: true
    use_raw_strings: true
    use_rethrow_when_possible: true
    use_setters_to_change_properties: true
    use_string_buffers: true
    use_string_in_part_of_directives: true

    # Experimental.
    use_super_parameters: true

    use_test_throws_matchers: true
    use_to_and_as_if_applicable: true
    use_truncating_division: true
    var_with_no_type_annotation: true
    void_checks: true

    # Pub rules: https://dart.dev/tools/linter-rules#pub-rules

    depend_on_referenced_packages: true
    package_names: true
    secure_pubspec_urls: true
    sort_pub_dependencies: true
