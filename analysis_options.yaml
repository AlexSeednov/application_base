# The package holds itself to the rule set it ships to consumers
# (lib/analysis_options.yaml). Preview fixes with `dart fix --dry-run`, apply
# them with `dart fix --apply`.
include: package:application_base/analysis_options.yaml